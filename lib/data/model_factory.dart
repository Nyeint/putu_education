final objectFactories = <Type, Function>{
  // LoginOb: (Map<String, dynamic> m) => LoginOb.fromJson(m),
  // // GenreModel: (Map<String, dynamic> m) => GenreModel.fromJson(m),
  // SongModel: (Map<String, dynamic> m) => SongModel.fromJson(m),
  // SongData: (Map<String, dynamic> m) => SongData.fromJson(m),
  // SongDetailModel: (Map<String, dynamic> m) => SongDetailModel.fromJson(m),
  //
  // GenreModel: (Map<String, dynamic> m) => GenreModel.fromJson(m),
  // GenreData: (Map<String, dynamic> m) => GenreData.fromJson(m),
  // GenreDetailModel: (Map<String, dynamic> m) => GenreDetailModel.fromJson(m),
  //
  // AlbumModel: (Map<String, dynamic> m) => AlbumModel.fromJson(m),
  // AlbumData: (Map<String, dynamic> m) => AlbumData.fromJson(m),
  // AlbumDetailModel: (Map<String, dynamic> m) => AlbumDetailModel.fromJson(m),
  //
  // ArtistModel: (Map<String, dynamic> m) => ArtistModel.fromJson(m),
  // ArtistData: (Map<String, dynamic> m) => ArtistData.fromJson(m),
  //
  // MusicModel: (Map<String, dynamic> m) => MusicModel.fromJson(m),
  // MusicData: (Map<String, dynamic> m) => MusicData.fromJson(m),
};
